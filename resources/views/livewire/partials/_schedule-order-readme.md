This component (ScheduleOrder) schedules the current draft order when toggled on.
It creates a scheduled order and immediately starts a new draft so users can keep selling.
OrderSidebar no longer owns the scheduling UI or flow.
